export default function War(first, second) {
    // index 0 = the cards that are the same
    // need next two cards from each player
    // whoever wins index 1 or 2 (automatically pick which is greater) wins all 6 cards
    // array.push to winner
}